/** Tiger idle / entrance timeline markers (CSS-driven; helpers for future GSAP). */
export const TIGER_IDLE = {
  breathMs: 2900,
  headMs: 3100,
  tailMs: 2400,
}

export const TIGER_WINNER = {
  enterMs: 1100,
  roarMs: 850,
  landMs: 550,
}
