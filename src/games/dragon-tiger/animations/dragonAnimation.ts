/** Dragon idle / entrance timeline markers (CSS-driven; helpers for future GSAP). */
export const DRAGON_IDLE = {
  breathMs: 3200,
  headMs: 3400,
  whiskerMs: 2200,
}

export const DRAGON_WINNER = {
  enterMs: 1150,
  roarMs: 900,
  mistMs: 1400,
}
